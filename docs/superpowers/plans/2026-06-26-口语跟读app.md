# 口语跟读 App Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 iPhone 上做一个"美剧台词跟读"工具——录原声、配台词、反复听、自己跟读（保留多次历史）、靠耳朵自我对比。

**Architecture:** 纯本地单机 iOS app。SwiftUI 做界面，SwiftData 持久化片段/跟读元数据，录音文件存沙盒 Documents 目录。音频录放封装成 `AudioService` 协议（含 fake 实现供单元测试），逻辑层可测、真机手动验证录放。

**Tech Stack:** Swift 5.9+ / SwiftUI / SwiftData / AVFoundation / XCTest，最低 iOS 17。

## Global Constraints

- 平台：iOS 原生，最低部署目标 **iOS 17.0**（SwiftData 要求）。
- 语言：Swift，UI 用 SwiftUI，持久化用 SwiftData（`@Model`）。
- 音频：用 AVFoundation（`AVAudioRecorder` / `AVAudioPlayer` / `AVAudioSession`）。
- 无后端、无联网、无第三方依赖（不引入 CocoaPods/SPM 外部包）。
- 录音文件存 app 沙盒 `Documents/recordings/` 目录，SwiftData 只存相对文件名。
- 所有可测逻辑走 XCTest；录放硬件部分通过 `AudioService` 协议 + fake 测试。
- 单元测试命令统一：
  `xcodebuild test -scheme Listen -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:ListenTests/<TestClass>`
  （本机已确认有 iPhone 17 模拟器、iOS 26.5 运行时；如换机器用 `xcrun simctl list devices` 里存在的机型名。）
- 项目名 `Listen`，bundle id `com.<你的前缀>.Listen`，主 target `Listen`，测试 target `ListenTests`。

---

### Task 1: Xcode 项目脚手架 + 冒烟测试

建好工程并让测试管道跑通。这是后续所有 TDD 的前提。

**Files:**
- Create: `Listen.xcodeproj`（由 Xcode 生成）
- Create: `Listen/ListenApp.swift`（Xcode 模板生成，稍后改）
- Create: `ListenTests/SmokeTests.swift`
- Test: `ListenTests/SmokeTests.swift`

**Interfaces:**
- Consumes: 无
- Produces: 一个可用 `xcodebuild test` 运行的 `Listen` scheme 与 `ListenTests` target。

- [ ] **Step 1: 在 Xcode 新建项目**

Xcode → File → New → Project → iOS → App。
- Product Name: `Listen`
- Interface: **SwiftUI**
- Language: **Swift**
- Storage: **None**（SwiftData 我们手写，不用模板的）
- 勾选 **Include Tests**
- 保存到 `/Users/liyang/listen`（与本 repo 同目录，使 `Listen.xcodeproj` 位于仓库根）。

新建后在 target `Listen` 的 General → Minimum Deployments 设为 **iOS 17.0**。

- [ ] **Step 2: 写冒烟测试**

替换 `ListenTests/SmokeTests.swift`（若模板生成的是 `ListenTests.swift`，重命名为 `SmokeTests.swift`）：

```swift
import XCTest
@testable import Listen

final class SmokeTests: XCTestCase {
    func test_harnessRuns() {
        XCTAssertEqual(1 + 1, 2)
    }
}
```

- [ ] **Step 3: 运行测试，确认管道可跑**

Run:
```bash
cd /Users/liyang/listen
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/SmokeTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "chore: scaffold Listen iOS project with smoke test"
```

---

### Task 2: 数据模型 Clip / Attempt / Collection

定义 SwiftData 模型并验证可建容器、可增删改查。

**Files:**
- Create: `Listen/Models/Clip.swift`
- Create: `Listen/Models/Attempt.swift`
- Create: `Listen/Models/PracticeCollection.swift`
- Create: `Listen/Models/ModelContainer+Preview.swift`
- Test: `ListenTests/ModelTests.swift`

**Interfaces:**
- Consumes: 无
- Produces:
  - `@Model final class Clip` 属性：`title: String`、`scriptText: String`、`originalAudioFilename: String`、`createdAt: Date`、`lastPracticedAt: Date?`、`attempts: [Attempt]`（`@Relationship(deleteRule: .cascade, inverse: \Attempt.clip)`）、`collection: PracticeCollection?`。初始化器 `init(title:scriptText:originalAudioFilename:createdAt:)`。
  - `@Model final class Attempt` 属性：`audioFilename: String`、`createdAt: Date`、`clip: Clip?`。初始化器 `init(audioFilename:createdAt:)`。
  - `@Model final class PracticeCollection` 属性：`name: String`、`clips: [Clip]`（`@Relationship(deleteRule: .nullify, inverse: \Clip.collection)`）。初始化器 `init(name:)`。
  - `func makeInMemoryContainer() throws -> ModelContainer`（测试与预览共用，内存存储）。

- [ ] **Step 1: 写失败测试**

`ListenTests/ModelTests.swift`：

```swift
import XCTest
import SwiftData
@testable import Listen

final class ModelTests: XCTestCase {
    @MainActor
    func test_insertClip_persistsAndFetches() throws {
        let container = try makeInMemoryContainer()
        let ctx = container.mainContext

        let clip = Clip(title: "How you doin'",
                        scriptText: "How you doin'?",
                        originalAudioFilename: "orig-1.m4a",
                        createdAt: Date(timeIntervalSince1970: 0))
        ctx.insert(clip)
        try ctx.save()

        let fetched = try ctx.fetch(FetchDescriptor<Clip>())
        XCTAssertEqual(fetched.count, 1)
        XCTAssertEqual(fetched.first?.title, "How you doin'")
        XCTAssertTrue(fetched.first?.attempts.isEmpty ?? false)
    }

    @MainActor
    func test_addAttempts_keepsHistory() throws {
        let container = try makeInMemoryContainer()
        let ctx = container.mainContext

        let clip = Clip(title: "t", scriptText: "s",
                        originalAudioFilename: "o.m4a",
                        createdAt: Date(timeIntervalSince1970: 0))
        ctx.insert(clip)
        clip.attempts.append(Attempt(audioFilename: "a1.m4a",
                                     createdAt: Date(timeIntervalSince1970: 1)))
        clip.attempts.append(Attempt(audioFilename: "a2.m4a",
                                     createdAt: Date(timeIntervalSince1970: 2)))
        try ctx.save()

        let fetched = try ctx.fetch(FetchDescriptor<Clip>()).first
        XCTAssertEqual(fetched?.attempts.count, 2)
    }

    @MainActor
    func test_deleteClip_cascadesAttempts() throws {
        let container = try makeInMemoryContainer()
        let ctx = container.mainContext

        let clip = Clip(title: "t", scriptText: "s",
                        originalAudioFilename: "o.m4a",
                        createdAt: Date(timeIntervalSince1970: 0))
        ctx.insert(clip)
        clip.attempts.append(Attempt(audioFilename: "a1.m4a",
                                     createdAt: Date(timeIntervalSince1970: 1)))
        try ctx.save()

        ctx.delete(clip)
        try ctx.save()

        XCTAssertEqual(try ctx.fetch(FetchDescriptor<Attempt>()).count, 0)
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/ModelTests
```
Expected: 编译失败（`Clip` / `Attempt` / `makeInMemoryContainer` 未定义）。

- [ ] **Step 3: 实现模型**

`Listen/Models/Attempt.swift`：

```swift
import Foundation
import SwiftData

@Model
final class Attempt {
    var audioFilename: String
    var createdAt: Date
    var clip: Clip?

    init(audioFilename: String, createdAt: Date) {
        self.audioFilename = audioFilename
        self.createdAt = createdAt
    }
}
```

`Listen/Models/PracticeCollection.swift`：

```swift
import Foundation
import SwiftData

@Model
final class PracticeCollection {
    var name: String
    @Relationship(deleteRule: .nullify, inverse: \Clip.collection)
    var clips: [Clip] = []

    init(name: String) {
        self.name = name
    }
}
```

`Listen/Models/Clip.swift`：

```swift
import Foundation
import SwiftData

@Model
final class Clip {
    var title: String
    var scriptText: String
    var originalAudioFilename: String
    var createdAt: Date
    var lastPracticedAt: Date?
    @Relationship(deleteRule: .cascade, inverse: \Attempt.clip)
    var attempts: [Attempt] = []
    var collection: PracticeCollection?

    init(title: String,
         scriptText: String,
         originalAudioFilename: String,
         createdAt: Date) {
        self.title = title
        self.scriptText = scriptText
        self.originalAudioFilename = originalAudioFilename
        self.createdAt = createdAt
    }
}
```

`Listen/Models/ModelContainer+Preview.swift`：

```swift
import Foundation
import SwiftData

@MainActor
func makeInMemoryContainer() throws -> ModelContainer {
    let config = ModelConfiguration(isStoredInMemoryOnly: true)
    return try ModelContainer(
        for: Clip.self, Attempt.self, PracticeCollection.self,
        configurations: config
    )
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/ModelTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: add Clip/Attempt/PracticeCollection SwiftData models"
```

---

### Task 3: 录音文件存储 RecordingStore

把"录音文件名 ↔ 沙盒路径"的读写隔离出来，纯逻辑可测。

**Files:**
- Create: `Listen/Audio/RecordingStore.swift`
- Test: `ListenTests/RecordingStoreTests.swift`

**Interfaces:**
- Consumes: 无
- Produces:
  - `struct RecordingStore`，`init(rootDirectory: URL)`。
  - `func url(for filename: String) -> URL`（拼到 `rootDirectory` 下）。
  - `func makeNewFilename(prefix: String) -> String`（返回形如 `<prefix>-<uuid>.m4a`，调用方传入 uuid 字符串以便测试确定性：实际签名 `makeFilename(prefix: String, id: String) -> String`，返回 `"\(prefix)-\(id).m4a"`）。
  - `func ensureRootExists() throws`（建目录）。
  - `func delete(filename: String) throws`（删文件，文件不存在不报错）。
  - `static func defaultStore() -> RecordingStore`（指向 `Documents/recordings/`）。

- [ ] **Step 1: 写失败测试**

`ListenTests/RecordingStoreTests.swift`：

```swift
import XCTest
@testable import Listen

final class RecordingStoreTests: XCTestCase {
    private func tempDir() -> URL {
        FileManager.default.temporaryDirectory
            .appendingPathComponent(UUID().uuidString, isDirectory: true)
    }

    func test_makeFilename_isDeterministic() {
        let store = RecordingStore(rootDirectory: tempDir())
        XCTAssertEqual(store.makeFilename(prefix: "orig", id: "ABC"),
                       "orig-ABC.m4a")
    }

    func test_url_joinsRoot() {
        let root = tempDir()
        let store = RecordingStore(rootDirectory: root)
        XCTAssertEqual(store.url(for: "x.m4a"),
                       root.appendingPathComponent("x.m4a"))
    }

    func test_ensureRootExists_createsDirectory() throws {
        let root = tempDir()
        let store = RecordingStore(rootDirectory: root)
        try store.ensureRootExists()
        var isDir: ObjCBool = false
        XCTAssertTrue(FileManager.default.fileExists(atPath: root.path,
                                                     isDirectory: &isDir))
        XCTAssertTrue(isDir.boolValue)
    }

    func test_delete_missingFile_doesNotThrow() throws {
        let store = RecordingStore(rootDirectory: tempDir())
        XCTAssertNoThrow(try store.delete(filename: "nope.m4a"))
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/RecordingStoreTests
```
Expected: 编译失败（`RecordingStore` 未定义）。

- [ ] **Step 3: 实现 RecordingStore**

`Listen/Audio/RecordingStore.swift`：

```swift
import Foundation

struct RecordingStore {
    let rootDirectory: URL

    func url(for filename: String) -> URL {
        rootDirectory.appendingPathComponent(filename)
    }

    func makeFilename(prefix: String, id: String) -> String {
        "\(prefix)-\(id).m4a"
    }

    func ensureRootExists() throws {
        try FileManager.default.createDirectory(
            at: rootDirectory, withIntermediateDirectories: true)
    }

    func delete(filename: String) throws {
        let target = url(for: filename)
        if FileManager.default.fileExists(atPath: target.path) {
            try FileManager.default.removeItem(at: target)
        }
    }

    static func defaultStore() -> RecordingStore {
        let docs = FileManager.default.urls(
            for: .documentDirectory, in: .userDomainMask)[0]
        return RecordingStore(
            rootDirectory: docs.appendingPathComponent("recordings",
                                                       isDirectory: true))
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/RecordingStoreTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: add RecordingStore for sandbox recording files"
```

---

### Task 4: AudioService 协议 + Fake + 真机实现

把录音/播放抽象成协议，逻辑层依赖协议；fake 用于测试，AVFoundation 实现用于真机。

**Files:**
- Create: `Listen/Audio/AudioService.swift`（协议 + 错误类型）
- Create: `Listen/Audio/AVAudioService.swift`（AVFoundation 真机实现）
- Create: `ListenTests/Fakes/FakeAudioService.swift`
- Test: `ListenTests/FakeAudioServiceTests.swift`

**Interfaces:**
- Consumes: `RecordingStore`（真机实现用它定位文件）
- Produces:
  - 协议 `protocol AudioService: AnyObject`：
    - `func startRecording(to filename: String) throws`
    - `func stopRecording()`
    - `func play(filename: String, loop: Bool) throws`
    - `func playSequence(_ filenames: [String]) throws`（依次播放，用于"原声→我的"对比）
    - `func stop()`
    - `var isRecording: Bool { get }`
    - `var isPlaying: Bool { get }`
  - `final class FakeAudioService: AudioService`，额外暴露：`recordedFilenames: [String]`、`playedFilenames: [String]`、`lastLoop: Bool`、`playedSequences: [[String]]`，用于断言。
  - `final class AVAudioService: AudioService`，`init(store: RecordingStore)`。

- [ ] **Step 1: 写 Fake 与失败测试**

`ListenTests/Fakes/FakeAudioService.swift`：

```swift
import Foundation
@testable import Listen

final class FakeAudioService: AudioService {
    private(set) var isRecording = false
    private(set) var isPlaying = false
    private(set) var recordedFilenames: [String] = []
    private(set) var playedFilenames: [String] = []
    private(set) var playedSequences: [[String]] = []
    private(set) var lastLoop = false

    func startRecording(to filename: String) throws {
        isRecording = true
        recordedFilenames.append(filename)
    }
    func stopRecording() { isRecording = false }
    func play(filename: String, loop: Bool) throws {
        isPlaying = true
        playedFilenames.append(filename)
        lastLoop = loop
    }
    func playSequence(_ filenames: [String]) throws {
        isPlaying = true
        playedSequences.append(filenames)
    }
    func stop() { isPlaying = false }
}
```

`ListenTests/FakeAudioServiceTests.swift`：

```swift
import XCTest
@testable import Listen

final class FakeAudioServiceTests: XCTestCase {
    func test_record_thenStop_tracksState() throws {
        let svc = FakeAudioService()
        try svc.startRecording(to: "orig-1.m4a")
        XCTAssertTrue(svc.isRecording)
        XCTAssertEqual(svc.recordedFilenames, ["orig-1.m4a"])
        svc.stopRecording()
        XCTAssertFalse(svc.isRecording)
    }

    func test_play_withLoop_tracksLoopFlag() throws {
        let svc = FakeAudioService()
        try svc.play(filename: "orig-1.m4a", loop: true)
        XCTAssertTrue(svc.isPlaying)
        XCTAssertTrue(svc.lastLoop)
        XCTAssertEqual(svc.playedFilenames, ["orig-1.m4a"])
    }

    func test_playSequence_recordsOrder() throws {
        let svc = FakeAudioService()
        try svc.playSequence(["orig-1.m4a", "a1.m4a"])
        XCTAssertEqual(svc.playedSequences, [["orig-1.m4a", "a1.m4a"]])
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/FakeAudioServiceTests
```
Expected: 编译失败（`AudioService` 协议未定义）。

- [ ] **Step 3: 实现协议与真机类**

`Listen/Audio/AudioService.swift`：

```swift
import Foundation

protocol AudioService: AnyObject {
    var isRecording: Bool { get }
    var isPlaying: Bool { get }
    func startRecording(to filename: String) throws
    func stopRecording()
    func play(filename: String, loop: Bool) throws
    func playSequence(_ filenames: [String]) throws
    func stop()
}

enum AudioServiceError: Error {
    case recorderUnavailable
    case fileNotFound(String)
}
```

`Listen/Audio/AVAudioService.swift`：

```swift
import Foundation
import AVFoundation

final class AVAudioService: NSObject, AudioService {
    private let store: RecordingStore
    private var recorder: AVAudioRecorder?
    private var player: AVAudioPlayer?
    private var sequenceQueue: [String] = []
    private var loopCurrent = false

    init(store: RecordingStore) {
        self.store = store
        super.init()
    }

    var isRecording: Bool { recorder?.isRecording ?? false }
    var isPlaying: Bool { player?.isPlaying ?? false }

    func startRecording(to filename: String) throws {
        try store.ensureRootExists()
        let session = AVAudioSession.sharedInstance()
        try session.setCategory(.playAndRecord, mode: .default,
                                options: [.defaultToSpeaker])
        try session.setActive(true)

        let settings: [String: Any] = [
            AVFormatIDKey: Int(kAudioFormatMPEG4AAC),
            AVSampleRateKey: 44100,
            AVNumberOfChannelsKey: 1,
            AVEncoderAudioQualityKey: AVAudioQuality.high.rawValue
        ]
        let rec = try AVAudioRecorder(url: store.url(for: filename),
                                      settings: settings)
        rec.record()
        recorder = rec
    }

    func stopRecording() {
        recorder?.stop()
        recorder = nil
    }

    func play(filename: String, loop: Bool) throws {
        try startPlayer(filename: filename, loop: loop)
    }

    func playSequence(_ filenames: [String]) throws {
        sequenceQueue = Array(filenames.dropFirst())
        guard let first = filenames.first else { return }
        loopCurrent = false
        try startPlayer(filename: first, loop: false)
    }

    func stop() {
        player?.stop()
        player = nil
        sequenceQueue = []
    }

    private func startPlayer(filename: String, loop: Bool) throws {
        let url = store.url(for: filename)
        guard FileManager.default.fileExists(atPath: url.path) else {
            throw AudioServiceError.fileNotFound(filename)
        }
        let session = AVAudioSession.sharedInstance()
        try session.setCategory(.playback, mode: .default)
        try session.setActive(true)
        let p = try AVAudioPlayer(contentsOf: url)
        p.delegate = self
        p.numberOfLoops = loop ? -1 : 0
        loopCurrent = loop
        p.play()
        player = p
    }
}

extension AVAudioService: AVAudioPlayerDelegate {
    func audioPlayerDidFinishPlaying(_ player: AVAudioPlayer,
                                     successfully flag: Bool) {
        guard !sequenceQueue.isEmpty else { return }
        let next = sequenceQueue.removeFirst()
        try? startPlayer(filename: next, loop: false)
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/FakeAudioServiceTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 在 Info 里加麦克风权限说明**

在 target `Listen` 的 Info（Info.plist 或 target Info 标签页）加一项：
- Key: `NSMicrophoneUsageDescription`（Privacy - Microphone Usage Description）
- Value: `用于录下你跟读的口语，方便和原声对比。`

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: add AudioService protocol with fake and AVFoundation impl"
```

---

### Task 5: 录音流程 ViewModel（RecordClipModel）

把"录原声 → 起名/填台词 → 存成 Clip + 文件"的逻辑放进可测的 ViewModel。

**Files:**
- Create: `Listen/Features/Record/RecordClipModel.swift`
- Test: `ListenTests/RecordClipModelTests.swift`

**Interfaces:**
- Consumes: `AudioService`、`RecordingStore`、SwiftData `ModelContext`、`Clip`
- Produces:
  - `@MainActor final class RecordClipModel: ObservableObject`
  - `init(audio: AudioService, store: RecordingStore, context: ModelContext, idProvider: @escaping () -> String = { UUID().uuidString }, now: @escaping () -> Date = Date.init)`
  - `@Published var title = ""`、`@Published var scriptText = ""`、`@Published var hasRecording = false`、`@Published var isRecording = false`
  - `func startRecording() throws`、`func stopRecording()`
  - `func save() throws -> Clip`（把录好的原声文件名 + 标题 + 台词写入新 `Clip` 并 insert+save；标题空时用 `"未命名"`；无录音时抛 `RecordError.noRecording`）
  - `enum RecordError: Error { case noRecording }`

- [ ] **Step 1: 写失败测试**

`ListenTests/RecordClipModelTests.swift`：

```swift
import XCTest
import SwiftData
@testable import Listen

@MainActor
final class RecordClipModelTests: XCTestCase {
    private func makeModel() throws
        -> (RecordClipModel, FakeAudioService, ModelContext) {
        let container = try makeInMemoryContainer()
        let audio = FakeAudioService()
        let store = RecordingStore(
            rootDirectory: FileManager.default.temporaryDirectory
                .appendingPathComponent(UUID().uuidString))
        var counter = 0
        let model = RecordClipModel(
            audio: audio, store: store, context: container.mainContext,
            idProvider: { counter += 1; return "ID\(counter)" },
            now: { Date(timeIntervalSince1970: 100) })
        return (model, audio, container.mainContext)
    }

    func test_save_withoutRecording_throws() throws {
        let (model, _, _) = try makeModel()
        XCTAssertThrowsError(try model.save())
    }

    func test_record_thenSave_createsClipWithFile() throws {
        let (model, audio, ctx) = try makeModel()
        try model.startRecording()
        XCTAssertTrue(model.isRecording)
        model.stopRecording()
        XCTAssertTrue(model.hasRecording)
        XCTAssertEqual(audio.recordedFilenames, ["orig-ID1.m4a"])

        model.title = "How you doin'"
        model.scriptText = "How you doin'?"
        let clip = try model.save()

        XCTAssertEqual(clip.originalAudioFilename, "orig-ID1.m4a")
        XCTAssertEqual(clip.title, "How you doin'")
        XCTAssertEqual(clip.scriptText, "How you doin'?")
        XCTAssertEqual(try ctx.fetch(FetchDescriptor<Clip>()).count, 1)
    }

    func test_save_emptyTitle_usesPlaceholder() throws {
        let (model, _, _) = try makeModel()
        try model.startRecording()
        model.stopRecording()
        let clip = try model.save()
        XCTAssertEqual(clip.title, "未命名")
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/RecordClipModelTests
```
Expected: 编译失败（`RecordClipModel` 未定义）。

- [ ] **Step 3: 实现 ViewModel**

`Listen/Features/Record/RecordClipModel.swift`：

```swift
import Foundation
import SwiftData

@MainActor
final class RecordClipModel: ObservableObject {
    enum RecordError: Error { case noRecording }

    @Published var title = ""
    @Published var scriptText = ""
    @Published var hasRecording = false
    @Published var isRecording = false

    private let audio: AudioService
    private let store: RecordingStore
    private let context: ModelContext
    private let idProvider: () -> String
    private let now: () -> Date
    private var pendingFilename: String?

    init(audio: AudioService,
         store: RecordingStore,
         context: ModelContext,
         idProvider: @escaping () -> String = { UUID().uuidString },
         now: @escaping () -> Date = Date.init) {
        self.audio = audio
        self.store = store
        self.context = context
        self.idProvider = idProvider
        self.now = now
    }

    func startRecording() throws {
        let filename = store.makeFilename(prefix: "orig", id: idProvider())
        try audio.startRecording(to: filename)
        pendingFilename = filename
        isRecording = true
        hasRecording = false
    }

    func stopRecording() {
        audio.stopRecording()
        isRecording = false
        hasRecording = pendingFilename != nil
    }

    func save() throws -> Clip {
        guard let filename = pendingFilename else {
            throw RecordError.noRecording
        }
        let finalTitle = title.trimmingCharacters(in: .whitespacesAndNewlines)
        let clip = Clip(
            title: finalTitle.isEmpty ? "未命名" : finalTitle,
            scriptText: scriptText,
            originalAudioFilename: filename,
            createdAt: now())
        context.insert(clip)
        try context.save()
        return clip
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/RecordClipModelTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: add RecordClipModel for recording and saving clips"
```

---

### Task 6: 练习流程 ViewModel（PracticeModel）

把"循环听原声 / 录跟读（新增 Attempt）/ 对比播放 / 删历史"的逻辑放进可测 ViewModel。

**Files:**
- Create: `Listen/Features/Practice/PracticeModel.swift`
- Test: `ListenTests/PracticeModelTests.swift`

**Interfaces:**
- Consumes: `AudioService`、`RecordingStore`、`ModelContext`、`Clip`、`Attempt`
- Produces:
  - `@MainActor final class PracticeModel: ObservableObject`
  - `init(clip: Clip, audio: AudioService, store: RecordingStore, context: ModelContext, idProvider: @escaping () -> String = { UUID().uuidString }, now: @escaping () -> Date = Date.init)`
  - `@Published var isRecording = false`
  - `func playOriginal(loop: Bool) throws`（播 `clip.originalAudioFilename`）
  - `func startAttempt() throws` / `func stopAttempt() throws`（停止时把新 `Attempt` 加到 `clip.attempts` 并 save，刷新 `clip.lastPracticedAt = now()`）
  - `func playAttempt(_ attempt: Attempt, loop: Bool) throws`
  - `func playComparison(_ attempt: Attempt) throws`（`playSequence([原声, attempt])`）
  - `func deleteAttempt(_ attempt: Attempt) throws`（删文件 + 从 context 删除 + save）
  - `var attemptsNewestFirst: [Attempt]`（按 createdAt 倒序）

- [ ] **Step 1: 写失败测试**

`ListenTests/PracticeModelTests.swift`：

```swift
import XCTest
import SwiftData
@testable import Listen

@MainActor
final class PracticeModelTests: XCTestCase {
    private func makeModel() throws
        -> (PracticeModel, FakeAudioService, Clip, ModelContext) {
        let container = try makeInMemoryContainer()
        let ctx = container.mainContext
        let clip = Clip(title: "t", scriptText: "s",
                        originalAudioFilename: "orig-1.m4a",
                        createdAt: Date(timeIntervalSince1970: 0))
        ctx.insert(clip)
        try ctx.save()
        let audio = FakeAudioService()
        let store = RecordingStore(
            rootDirectory: FileManager.default.temporaryDirectory
                .appendingPathComponent(UUID().uuidString))
        var counter = 0
        let model = PracticeModel(
            clip: clip, audio: audio, store: store, context: ctx,
            idProvider: { counter += 1; return "ID\(counter)" },
            now: { Date(timeIntervalSince1970: 50) })
        return (model, audio, clip, ctx)
    }

    func test_playOriginal_loops() throws {
        let (model, audio, _, _) = try makeModel()
        try model.playOriginal(loop: true)
        XCTAssertEqual(audio.playedFilenames, ["orig-1.m4a"])
        XCTAssertTrue(audio.lastLoop)
    }

    func test_recordAttempt_appendsHistory() throws {
        let (model, audio, clip, _) = try makeModel()
        try model.startAttempt()
        XCTAssertTrue(model.isRecording)
        try model.stopAttempt()
        XCTAssertFalse(model.isRecording)
        XCTAssertEqual(clip.attempts.count, 1)
        XCTAssertEqual(clip.attempts.first?.audioFilename, "mine-ID1.m4a")
        XCTAssertEqual(audio.recordedFilenames, ["mine-ID1.m4a"])
        XCTAssertEqual(clip.lastPracticedAt, Date(timeIntervalSince1970: 50))
    }

    func test_playComparison_playsOriginalThenAttempt() throws {
        let (model, audio, _, _) = try makeModel()
        try model.startAttempt()
        try model.stopAttempt()
        let attempt = model.attemptsNewestFirst[0]
        try model.playComparison(attempt)
        XCTAssertEqual(audio.playedSequences,
                       [["orig-1.m4a", "mine-ID1.m4a"]])
    }

    func test_deleteAttempt_removesFromHistory() throws {
        let (model, _, clip, _) = try makeModel()
        try model.startAttempt()
        try model.stopAttempt()
        let attempt = model.attemptsNewestFirst[0]
        try model.deleteAttempt(attempt)
        XCTAssertEqual(clip.attempts.count, 0)
    }

    func test_attemptsNewestFirst_ordering() throws {
        let (model, _, clip, ctx) = try makeModel()
        clip.attempts.append(Attempt(audioFilename: "a.m4a",
                                     createdAt: Date(timeIntervalSince1970: 1)))
        clip.attempts.append(Attempt(audioFilename: "b.m4a",
                                     createdAt: Date(timeIntervalSince1970: 9)))
        try ctx.save()
        XCTAssertEqual(model.attemptsNewestFirst.map(\.audioFilename),
                       ["b.m4a", "a.m4a"])
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/PracticeModelTests
```
Expected: 编译失败（`PracticeModel` 未定义）。

- [ ] **Step 3: 实现 ViewModel**

`Listen/Features/Practice/PracticeModel.swift`：

```swift
import Foundation
import SwiftData

@MainActor
final class PracticeModel: ObservableObject {
    @Published var isRecording = false

    let clip: Clip
    private let audio: AudioService
    private let store: RecordingStore
    private let context: ModelContext
    private let idProvider: () -> String
    private let now: () -> Date
    private var pendingFilename: String?

    init(clip: Clip,
         audio: AudioService,
         store: RecordingStore,
         context: ModelContext,
         idProvider: @escaping () -> String = { UUID().uuidString },
         now: @escaping () -> Date = Date.init) {
        self.clip = clip
        self.audio = audio
        self.store = store
        self.context = context
        self.idProvider = idProvider
        self.now = now
    }

    var attemptsNewestFirst: [Attempt] {
        clip.attempts.sorted { $0.createdAt > $1.createdAt }
    }

    func playOriginal(loop: Bool) throws {
        try audio.play(filename: clip.originalAudioFilename, loop: loop)
    }

    func startAttempt() throws {
        let filename = store.makeFilename(prefix: "mine", id: idProvider())
        try audio.startRecording(to: filename)
        pendingFilename = filename
        isRecording = true
    }

    func stopAttempt() throws {
        audio.stopRecording()
        isRecording = false
        guard let filename = pendingFilename else { return }
        let attempt = Attempt(audioFilename: filename, createdAt: now())
        clip.attempts.append(attempt)
        clip.lastPracticedAt = now()
        try context.save()
        pendingFilename = nil
    }

    func playAttempt(_ attempt: Attempt, loop: Bool) throws {
        try audio.play(filename: attempt.audioFilename, loop: loop)
    }

    func playComparison(_ attempt: Attempt) throws {
        try audio.playSequence([clip.originalAudioFilename,
                                attempt.audioFilename])
    }

    func deleteAttempt(_ attempt: Attempt) throws {
        try? store.delete(filename: attempt.audioFilename)
        context.delete(attempt)
        try context.save()
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/PracticeModelTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "feat: add PracticeModel for listen/record/compare/history"
```

---

### Task 7: 接线 App 入口 + 片段库首页 + 录制页 + 练习页

把前面纯逻辑层接到 SwiftUI 界面，跑通最小闭环。界面靠真机/模拟器手动验证（无单元测试）。

**Files:**
- Modify: `Listen/ListenApp.swift`
- Create: `Listen/Features/Library/ClipLibraryView.swift`
- Create: `Listen/Features/Record/RecordView.swift`
- Create: `Listen/Features/Practice/PracticeView.swift`
- Create: `Listen/AppEnvironment.swift`（共享 `AudioService` + `RecordingStore`）

**Interfaces:**
- Consumes: `RecordClipModel`、`PracticeModel`、`Clip`、`AVAudioService`、`RecordingStore`
- Produces: 可运行的 app；首页列出片段，可录新片段、可进入练习。

- [ ] **Step 1: App 入口接 SwiftData 容器与音频环境**

`Listen/AppEnvironment.swift`：

```swift
import Foundation

@MainActor
final class AppEnvironment {
    let store: RecordingStore
    let audio: AudioService

    init() {
        let store = RecordingStore.defaultStore()
        self.store = store
        self.audio = AVAudioService(store: store)
    }
}
```

`Listen/ListenApp.swift`：

```swift
import SwiftUI
import SwiftData

@main
struct ListenApp: App {
    let container: ModelContainer
    let environment = AppEnvironment()

    init() {
        do {
            container = try ModelContainer(
                for: Clip.self, Attempt.self, PracticeCollection.self)
        } catch {
            fatalError("无法创建 ModelContainer: \(error)")
        }
    }

    var body: some Scene {
        WindowGroup {
            ClipLibraryView(environment: environment)
        }
        .modelContainer(container)
    }
}
```

- [ ] **Step 2: 片段库首页**

`Listen/Features/Library/ClipLibraryView.swift`：

```swift
import SwiftUI
import SwiftData

struct ClipLibraryView: View {
    let environment: AppEnvironment
    @Environment(\.modelContext) private var context
    @Query(sort: \Clip.createdAt, order: .reverse) private var clips: [Clip]
    @State private var showingRecord = false

    var body: some View {
        NavigationStack {
            List {
                ForEach(clips) { clip in
                    NavigationLink {
                        PracticeView(model: PracticeModel(
                            clip: clip,
                            audio: environment.audio,
                            store: environment.store,
                            context: context))
                    } label: {
                        VStack(alignment: .leading, spacing: 4) {
                            Text(clip.title).font(.headline)
                            Text(clip.scriptText)
                                .font(.subheadline)
                                .foregroundStyle(.secondary)
                                .lineLimit(1)
                        }
                    }
                }
                .onDelete(perform: deleteClips)
            }
            .navigationTitle("片段库")
            .toolbar {
                ToolbarItem(placement: .topBarTrailing) {
                    Button {
                        showingRecord = true
                    } label: {
                        Label("录新片段", systemImage: "plus")
                    }
                }
            }
            .overlay {
                if clips.isEmpty {
                    ContentUnavailableView(
                        "还没有片段",
                        systemImage: "mic",
                        description: Text("点右上角 ＋ 录下第一句美剧对话"))
                }
            }
            .sheet(isPresented: $showingRecord) {
                RecordView(model: RecordClipModel(
                    audio: environment.audio,
                    store: environment.store,
                    context: context))
            }
        }
    }

    private func deleteClips(_ offsets: IndexSet) {
        for index in offsets {
            let clip = clips[index]
            try? environment.store.delete(filename: clip.originalAudioFilename)
            for attempt in clip.attempts {
                try? environment.store.delete(filename: attempt.audioFilename)
            }
            context.delete(clip)
        }
        try? context.save()
    }
}
```

- [ ] **Step 3: 录制页**

`Listen/Features/Record/RecordView.swift`：

```swift
import SwiftUI

struct RecordView: View {
    @StateObject var model: RecordClipModel
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        NavigationStack {
            Form {
                Section("原声录音") {
                    Button {
                        toggleRecording()
                    } label: {
                        Label(model.isRecording ? "停止录音" : "开始录音",
                              systemImage: model.isRecording
                                  ? "stop.circle.fill" : "mic.circle.fill")
                            .foregroundStyle(model.isRecording ? .red : .accentColor)
                    }
                    if model.hasRecording {
                        Text("已录好 ✓").foregroundStyle(.secondary)
                    }
                }
                Section("台词") {
                    TextField("名字，如 How you doin'", text: $model.title)
                    TextField("英文台词", text: $model.scriptText,
                              axis: .vertical)
                        .lineLimit(2...5)
                }
            }
            .navigationTitle("录新片段")
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("取消") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("保存") {
                        _ = try? model.save()
                        dismiss()
                    }
                    .disabled(!model.hasRecording)
                }
            }
        }
    }

    private func toggleRecording() {
        if model.isRecording {
            model.stopRecording()
        } else {
            try? model.startRecording()
        }
    }
}
```

- [ ] **Step 4: 练习页**

`Listen/Features/Practice/PracticeView.swift`：

```swift
import SwiftUI

struct PracticeView: View {
    @StateObject var model: PracticeModel

    var body: some View {
        List {
            Section {
                Text(model.clip.scriptText.isEmpty
                     ? "（无台词）" : model.clip.scriptText)
                    .font(.title2)
                    .frame(maxWidth: .infinity, alignment: .leading)
                    .padding(.vertical, 8)
            }

            Section("练习") {
                Button {
                    try? model.playOriginal(loop: true)
                } label: {
                    Label("循环听原声", systemImage: "repeat")
                }
                Button {
                    toggleAttempt()
                } label: {
                    Label(model.isRecording ? "停止录音" : "录我的跟读",
                          systemImage: model.isRecording
                              ? "stop.circle.fill" : "mic.circle.fill")
                        .foregroundStyle(model.isRecording ? .red : .accentColor)
                }
            }

            Section("历史跟读（\(model.attemptsNewestFirst.count)）") {
                if model.attemptsNewestFirst.isEmpty {
                    Text("还没有跟读，先录一条吧")
                        .foregroundStyle(.secondary)
                }
                ForEach(model.attemptsNewestFirst) { attempt in
                    HStack {
                        Button {
                            try? model.playAttempt(attempt, loop: false)
                        } label: {
                            Image(systemName: "play.circle")
                        }
                        Text(attempt.createdAt, style: .time)
                        Spacer()
                        Button("对比") {
                            try? model.playComparison(attempt)
                        }
                        .buttonStyle(.bordered)
                    }
                    .swipeActions {
                        Button("删除", role: .destructive) {
                            try? model.deleteAttempt(attempt)
                        }
                    }
                }
            }
        }
        .navigationTitle(model.clip.title)
    }

    private func toggleAttempt() {
        if model.isRecording {
            try? model.stopAttempt()
        } else {
            try? model.startAttempt()
        }
    }
}
```

- [ ] **Step 5: 编译 + 真机/模拟器手动验证最小闭环**

Run（先确认编译通过）：
```bash
xcodebuild build -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
Expected: `** BUILD SUCCEEDED **`

然后在**真机**（模拟器麦克风可用但外放录制需真机更可靠）手动走查清单：
1. 启动 → 看到空状态提示。
2. ＋ → 开始录音（首次弹麦克风授权，允许）→ 对着外放的美剧录几秒 → 停止 → 填名字/台词 → 保存。
3. 首页出现该片段 → 点进练习页。
4. 「循环听原声」能反复播放。
5. 「录我的跟读」录一条 → 历史出现 1 条。
6. 点历史条目播放、点「对比」听到 原声→我的 连续播放。
7. 左滑删除历史条目，条目消失。
8. 首页左滑删除片段，片段消失。

- [ ] **Step 6: 提交**

```bash
git add -A
git commit -m "feat: wire up library, record, and practice screens (minimal loop)"
```

---

### Task 8（里程碑 2，可选）: 台词/名字编辑 + 合集分组

最小闭环跑通后再做。给 `PracticeView` 加编辑入口，给首页加按合集分组。

**Files:**
- Create: `Listen/Features/Practice/EditClipView.swift`
- Modify: `Listen/Features/Practice/PracticeView.swift`（加「编辑」工具栏按钮）
- Modify: `Listen/Features/Library/ClipLibraryView.swift`（按 `collection.name` 分组显示）
- Create: `ListenTests/EditClipModelTests.swift`
- Create: `Listen/Features/Practice/EditClipModel.swift`

**Interfaces:**
- Consumes: `Clip`、`PracticeCollection`、`ModelContext`
- Produces:
  - `@MainActor final class EditClipModel: ObservableObject`，`init(clip: Clip, context: ModelContext)`，`@Published var title`、`@Published var scriptText`，`func save() throws`（写回 clip 并 save）。
  - `EditClipView` 表单；`ClipLibraryView` 用 `SectionedFetchRequest` 或在内存里 `Dictionary(grouping:)` 按合集名分组。

- [ ] **Step 1: 写 EditClipModel 失败测试**

`ListenTests/EditClipModelTests.swift`：

```swift
import XCTest
import SwiftData
@testable import Listen

@MainActor
final class EditClipModelTests: XCTestCase {
    func test_save_writesBackTitleAndScript() throws {
        let container = try makeInMemoryContainer()
        let ctx = container.mainContext
        let clip = Clip(title: "old", scriptText: "old script",
                        originalAudioFilename: "o.m4a",
                        createdAt: Date(timeIntervalSince1970: 0))
        ctx.insert(clip)
        try ctx.save()

        let model = EditClipModel(clip: clip, context: ctx)
        model.title = "new"
        model.scriptText = "new script"
        try model.save()

        let fetched = try ctx.fetch(FetchDescriptor<Clip>()).first
        XCTAssertEqual(fetched?.title, "new")
        XCTAssertEqual(fetched?.scriptText, "new script")
    }
}
```

- [ ] **Step 2: 运行测试，确认失败**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/EditClipModelTests
```
Expected: 编译失败（`EditClipModel` 未定义）。

- [ ] **Step 3: 实现 EditClipModel**

`Listen/Features/Practice/EditClipModel.swift`：

```swift
import Foundation
import SwiftData

@MainActor
final class EditClipModel: ObservableObject {
    @Published var title: String
    @Published var scriptText: String
    private let clip: Clip
    private let context: ModelContext

    init(clip: Clip, context: ModelContext) {
        self.clip = clip
        self.context = context
        self.title = clip.title
        self.scriptText = clip.scriptText
    }

    func save() throws {
        let trimmed = title.trimmingCharacters(in: .whitespacesAndNewlines)
        clip.title = trimmed.isEmpty ? "未命名" : trimmed
        clip.scriptText = scriptText
        try context.save()
    }
}
```

- [ ] **Step 4: 运行测试，确认通过**

Run:
```bash
xcodebuild test -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17' \
  -only-testing:ListenTests/EditClipModelTests
```
Expected: `** TEST SUCCEEDED **`

- [ ] **Step 5: 实现 EditClipView 并接入练习页**

`Listen/Features/Practice/EditClipView.swift`：

```swift
import SwiftUI

struct EditClipView: View {
    @StateObject var model: EditClipModel
    @Environment(\.dismiss) private var dismiss

    var body: some View {
        NavigationStack {
            Form {
                TextField("名字", text: $model.title)
                TextField("英文台词", text: $model.scriptText, axis: .vertical)
                    .lineLimit(2...6)
            }
            .navigationTitle("编辑片段")
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("取消") { dismiss() }
                }
                ToolbarItem(placement: .confirmationAction) {
                    Button("保存") {
                        try? model.save()
                        dismiss()
                    }
                }
            }
        }
    }
}
```

在 `PracticeView` 的 `.navigationTitle(...)` 后追加：

```swift
        .toolbar {
            ToolbarItem(placement: .topBarTrailing) {
                Button("编辑") { showingEdit = true }
            }
        }
        .sheet(isPresented: $showingEdit) {
            EditClipView(model: EditClipModel(
                clip: model.clip, context: editContext))
        }
```

并在 `PracticeView` 顶部加：
```swift
    @Environment(\.modelContext) private var editContext
    @State private var showingEdit = false
```

- [ ] **Step 6: 首页按合集分组**

在 `ClipLibraryView` 把单一 `List(clips)` 改为按合集名分组（无合集归入「未分组」）：

```swift
            List {
                ForEach(groupedClips, id: \.0) { name, group in
                    Section(name) {
                        ForEach(group) { clip in
                            NavigationLink {
                                PracticeView(model: PracticeModel(
                                    clip: clip,
                                    audio: environment.audio,
                                    store: environment.store,
                                    context: context))
                            } label: {
                                VStack(alignment: .leading, spacing: 4) {
                                    Text(clip.title).font(.headline)
                                    Text(clip.scriptText)
                                        .font(.subheadline)
                                        .foregroundStyle(.secondary)
                                        .lineLimit(1)
                                }
                            }
                        }
                    }
                }
            }
```

并加计算属性：
```swift
    private var groupedClips: [(String, [Clip])] {
        Dictionary(grouping: clips) { $0.collection?.name ?? "未分组" }
            .map { ($0.key, $0.value) }
            .sorted { $0.0 < $1.0 }
    }
```

（合集的创建/分配 UI 可作为后续迭代；此步先让"未分组"正常显示，不阻塞。）

- [ ] **Step 7: 编译 + 手动验证**

Run:
```bash
xcodebuild build -scheme Listen \
  -destination 'platform=iOS Simulator,name=iPhone 17'
```
Expected: `** BUILD SUCCEEDED **`

手动：进练习页 → 编辑 → 改名字/台词 → 保存 → 首页与练习页标题更新；列表显示「未分组」分区。

- [ ] **Step 8: 提交**

```bash
git add -A
git commit -m "feat: add clip editing and collection grouping"
```

---

## 任务依赖与顺序

1 →（2, 3 可并行）→ 4 → 5 → 6 → 7（最小可用 app 在此完成）→ 8（可选迭代）。

Task 5 依赖 2/3/4；Task 6 依赖 2/3/4；Task 7 依赖 5/6；Task 8 依赖 7。

## 验收（最小闭环）

完成 Task 7 后，在真机上能：录原声 → 填台词 → 循环听 → 录跟读（多条历史）→
单条回放 → 原声/我的对比播放 → 删历史 / 删片段。全部本地、无联网。
